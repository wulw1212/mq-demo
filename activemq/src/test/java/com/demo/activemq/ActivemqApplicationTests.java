/*
package com.demo.activemq;

import com.demo.activemq.queue.Customer;
import com.demo.activemq.queue.Provider;
import org.apache.activemq.command.ActiveMQQueue;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import javax.jms.Destination;
import java.util.Date;


@RunWith(SpringRunner.class)
@SpringBootTest(classes = com.demo.activemq.ActivemqApplication.class)
public class ActivemqApplicationTests {

    @Autowired
    private Customer customer;

    @Autowired
    private Provider provider;

    @Test
    public void contextLoads() throws InterruptedException {
        */
/*Destination destination = new ActiveMQQueue("mytest.queue");
        String msg;
        for(int i= 0;i<10;i++){
            msg="  i = ("+i+") activemq = > "+new Date();
            Thread.sleep(1000);
            provider.sender(destination,msg);
        }*//*

    }




}
*/
